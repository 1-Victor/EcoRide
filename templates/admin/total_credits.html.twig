{# templates/admin/credits.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Crédits | EcoRide{% endblock %}

{% block body %}
{% include 'components/menu_admin.html.twig' %}

<div class="container py-5">
    <div class="text-center mb-5">
        <h1 class="display-5 fw-bold">💰 Statistiques des Crédits</h1>
        <p class="text-muted fs-5">Récapitulatif complet des crédits gagnés et perdus par jour.</p>
    </div>

    <div class="row g-4">
        <!-- Crédits gagnés -->
        <div class="col-lg-6">
            <div class="card shadow border-success">
                <div class="card-header bg-success text-white d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">💸 Crédits gagnés</h5>
                    <span class="badge bg-light text-success fs-6">Total : {{ totalGained }} crédits</span>
                </div>
                <div class="card-body">
                    <table class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th>Date</th>
                                <th>Nombre de trajets</th>
                                <th>Crédits gagnés</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for item in gains %}
                            <tr>
                                <td>{{ item.date }}</td>
                                <td>{{ item.count }}</td>
                                <td>{{ item.credits }}</td>
                            </tr>
                        {% else %}
                            <tr><td colspan="3" class="text-center text-muted">Aucun trajet enregistré.</td></tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Crédits perdus -->
        <div class="col-lg-6">
            <div class="card shadow border-danger">
                <div class="card-header bg-danger text-white d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">⛔ Crédits perdus</h5>
                    <span class="badge bg-light text-danger fs-6">Total : {{ totalLost }} crédits</span>
                </div>
                <div class="card-body">
                    <table class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th>Date</th>
                                <th>Annulations</th>
                                <th>Crédits perdus</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for item in losses %}
                            <tr>
                                <td>{{ item.date }}</td>
                                <td>{{ item.count }}</td>
                                <td>{{ item.credits }}</td>
                            </tr>
                        {% else %}
                            <tr><td colspan="3" class="text-center text-muted">Aucune annulation enregistrée.</td></tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
